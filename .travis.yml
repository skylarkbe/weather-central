language: java
jdk:
  - oraclejdk8

sudo: false

install: mvn install -DskipTests=true -Dmaven.javadoc.skip=true -B -V
script: mvn test -DdarkSky.api.key=APIKEY -B

cache:
  directories:
    - ~/.m2/repository